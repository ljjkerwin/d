const assert = require('assert');



describe('modules/utils', () => {
  it('111', () => {
    throw Error('no')
  })

  it('formatDate', () => {
    assert.equal(1, true);
  })
})


describe('modules/localdb', () => {
  it('get', () => {
    assert.equal(1, true);
  })
})